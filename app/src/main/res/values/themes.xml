<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Coursr" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
    </style>
    <style name="Theme.Coursr" parent="Base.Theme.Coursr" />
    <style name="Widget.Material3.BottomNavigationView.ActiveIndicator." parent="">
        <item name="android:width">64dp</item>
        <item name="android:height">32dp</item>
        <item name="shapeAppearance">
            @style/ShapeAppearance.Material3.NavigationBarView.ActiveIndicator
        </item>
        <item name="android:color">?attr/colorSecondaryContainer</item>
    </style>
    <style name="AppCustomIndicator" parent="Widget.Material3.BottomNavigationView.ActiveIndicator.">
        <item name="android:color">@color/stroke</item>
    </style>
</resources>